fastapi==0.115.5
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
pydantic==2.10.3
httpx==0.28.1
python-multipart==0.0.19
jinja2==3.1.4
# Used to validate generated nuclei templates before handing them over.
# templategen builds YAML by concatenation, and a finding's name is
# arbitrary text from a scanned site — parsing the result catches a
# malformed file here rather than when nuclei refuses to load it.
PyYAML==6.0.2
